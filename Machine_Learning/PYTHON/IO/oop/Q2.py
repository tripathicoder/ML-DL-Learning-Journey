class Student:
    def __init__(self, name, marks):
        self.name=name
        self.marks=marks
        def get(self):
            sum=0;
            for val in marks:
                sum+=val
                print("your name is:",self.name,sum/3)
                s1=Student("Alice",[80,90,85])
                s1.get()
                
                
        